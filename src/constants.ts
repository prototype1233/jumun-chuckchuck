/** 앱 전체에서 함께 쓰는 값 */

/**
 * 카드를 누른 뒤 다음 화면으로 넘어가기까지 기다리는 시간(ms).
 *
 * 자동 타임아웃이 아니라, 누른 것이 제대로 선택됐는지
 * (테두리 + 배경 + 체크 표시) 눈으로 확인할 틈을 주기 위한 연출이다.
 * 화면마다 반응 속도가 다르면 혼란스러우므로 모든 선택 화면이 이 값을 같이 쓴다.
 */
export const AUTO_ADVANCE_MS = 400
